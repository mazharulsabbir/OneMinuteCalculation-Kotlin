package edu.wonder.youth.math.game.utils

import kotlin.math.roundToInt

class Maths {

    fun number1(range: Int): Int {
        return (Math.random() * range).roundToInt()
    }

    fun number2(range: Int): Int {
        return (Math.random() * range).roundToInt()
    }

    fun number3(range: Int): Int {
        return (Math.random() * range).roundToInt()
    }

    fun number4(range: Int): Int {
        return (Math.random() * range).roundToInt()
    }

}
